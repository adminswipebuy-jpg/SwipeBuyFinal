# V9.9 AI Agent Economy 2.0

Collections:
- `agent_purchase_requests`
- `users/{uid}/agent_purchases`
- `users/{uid}/agent_earnings`
- `agent_payout_requests`

The mobile client creates requests and reads trusted ledger data. It must not be the source of truth for successful payments, balances, fees or completed payouts.
