# SwipeBuy V4.8

Rich messaging, media attachments, reactions, read receipts, typing state, communities, community media posts, polls and moderation/reporting hooks.

Start from the `v48/` Flutter project. Configure Firebase with the existing `firebase_options.dart` and deploy Firestore/Storage rules only after reviewing them for the final schema.
