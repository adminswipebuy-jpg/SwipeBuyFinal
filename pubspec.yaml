description: SwipeBuy V1 Secure - global vertical-video marketplace starter.
publish_to: "none"
version: 15.0.0+88

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  http: ^1.2.2
  firebase_messaging: ^16.6.0
  google_maps_flutter: ^2.10.0
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.0
  cloud_firestore: ^6.1.0
  cloud_functions: ^6.1.0
  firebase_storage: ^13.0.0
  firebase_app_check: ^0.4.1
  image_picker: ^1.1.2
  video_player: ^2.9.2
  google_mobile_ads: ^9.1.0
  speech_to_text: ^7.4.0
  geolocator: ^14.0.3

dev_dependencies:
  http: ^1.2.2
  google_maps_flutter: ^2.10.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
